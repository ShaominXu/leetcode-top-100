# [无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters/)

## 思路

使用滑动窗口。维护左边界 `left` 和哈希表 `last`（记录字符最近出现位置）。
遍历到下标 `right` 时，如果当前字符在窗口内出现过，则把 `left` 更新到重复字符上次位置的下一位。
窗口长度为 `right - left + 1`，遍历过程中维护最大值。

## 代码

=== "Python3"
    ```python
    class Solution:
        def lengthOfLongestSubstring(self, s: str) -> int:
            last = {}
            left = 0
            ans = 0
            for right, ch in enumerate(s):
                if ch in last and last[ch] >= left:
                    left = last[ch] + 1
                last[ch] = right
                ans = max(ans, right - left + 1)
            return ans
    ```

=== "C++"
    ```cpp

    class Solution {
    public:
        int lengthOfLongestSubstring(string s) {
            unordered_map<char, int> last;
            int left = 0, ans = 0;
            for (int right = 0; right < (int)s.size(); ++right) {
                char ch = s[right];
                if (last.count(ch) && last[ch] >= left) {
                    left = last[ch] + 1;
                }
                last[ch] = right;
                ans = max(ans, right - left + 1);
            }
            return ans;
        }
    };
    ```

### 复杂度分析

时间复杂度：O(n)，因为主要流程对数据做了一次线性处理。
空间复杂度：O(min(n, |Sigma|)，额外空间主要来自哈希表/队列/递归栈等辅助结构。
