# 53. 最大子数组和

- LeetCode：[最大子数组和](https://leetcode.cn/problems/maximum-subarray/)
- 分类：动态规划 / Kadane

## 题意

给定整数数组 `nums`，求**连续**子数组之和的最大值（子数组至少包含一个元素）。

## 思路（Kadane / 线性 DP）

- 设 `cur` 为「以当前元素结尾」的子数组的最大和。
- 遍历每个 `x`：要么把 `x` 接在上一段后面（`cur + x`），要么从 `x` 重新开始一段（`x`），取较大者：  
  `cur = max(x, cur + x)`。
- 全局答案 `ans` 为所有位置 `cur` 的最大值。

时间复杂度 \(O(n)\)，空间 \(O(1)\)。

## 代码（支持语言切换）

<div class="lang-switch" data-problem="53-max-subarray">
    <button type="button" data-lang="python" class="active">Python</button>
    <button type="button" data-lang="cpp">C++</button>
</div>

<div class="lang-panel" data-problem="53-max-subarray" data-lang="python">

<pre><code class="language-python">from typing import List


class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        cur = ans = nums[0]
        for x in nums[1:]:
            cur = max(x, cur + x)
            ans = max(ans, cur)
        return ans
</code></pre>

</div>

<div class="lang-panel" data-problem="53-max-subarray" data-lang="cpp" style="display: none;">

<pre><code class="language-cpp">#include &lt;vector&gt;
#include &lt;algorithm&gt;
using namespace std;


class Solution {
public:
    int maxSubArray(vector&lt;int&gt;&amp; nums) {
        int cur = nums[0], ans = nums[0];
        for (int i = 1; i &lt; (int)nums.size(); ++i) {
            cur = max(nums[i], cur + nums[i]);
            ans = max(ans, cur);
        }
        return ans;
    }
};
</code></pre>

</div>

<style>
    .lang-switch {
        display: inline-flex;
        gap: 8px;
        margin: 8px 0 12px;
    }

    .lang-switch button {
        border: 1px solid #d0d7de;
        background: #f6f8fa;
        color: #24292f;
        border-radius: 8px;
        padding: 4px 12px;
        cursor: pointer;
        font-size: 14px;
    }

    .lang-switch button.active {
        background: #0969da;
        color: #ffffff;
        border-color: #0969da;
    }
</style>

<script>
    (function () {
        const problemId = '53-max-subarray';
        const group = document.querySelector(`.lang-switch[data-problem="${problemId}"]`);
        if (!group) return;

        const buttons = group.querySelectorAll('button[data-lang]');
        const panels = document.querySelectorAll(`.lang-panel[data-problem="${problemId}"]`);

        function switchLang(lang) {
            buttons.forEach((btn) => {
                btn.classList.toggle('active', btn.dataset.lang === lang);
            });
            panels.forEach((panel) => {
                panel.style.display = panel.dataset.lang === lang ? '' : 'none';
            });
        }

        buttons.forEach((btn) => {
            btn.addEventListener('click', () => switchLang(btn.dataset.lang));
        });

        switchLang('python');
    })();
</script>

## 等价 DP 写法

`dp[i]` 表示以 `i` 结尾的最大子数组和：

```python
class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        n = len(nums)
        dp = [0] * n
        dp[0] = nums[0]
        ans = dp[0]
        for i in range(1, n):
            dp[i] = max(nums[i], dp[i - 1] + nums[i])
            ans = max(ans, dp[i])
        return ans
```

上式可滚动为上一段代码，因为 `dp[i]` 只依赖 `dp[i-1]`。

---

[← 返回站点首页](../../index.html) · [← 页内「动态规划」](../../index.html#topic-dp)
